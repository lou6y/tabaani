{% extends 'headerfooterFRONTACCEUIL2.html.twig' %}
{% block content %}
{% block title %}Reservation{% endblock %}

{% block body %}
    <h1>Reservation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdReservation</th>
                <td>{{ reservation.idReservation }}</td>
            </tr>
            <tr>
                <th>TypeVehicule</th>
                <td>{{ reservation.typeVehicule }}</td>
            </tr>
            <tr>
                <th>Prise</th>
                <td>{{ reservation.prise ? reservation.prise|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Remise</th>
                <td>{{ reservation.remise ? reservation.remise|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>DateDebut</th>
                <td>{{ reservation.dateDebut ? reservation.dateDebut|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>DateFin</th>
                <td>{{ reservation.dateFin ? reservation.dateFin|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>IdTransport</th>
                <td>{{ reservation.idTransport }}</td>
            </tr>
            <tr>
                <th>IdUser</th>
                <td>{{ reservation.idUser }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('reservation_index') }}">back to list</a>

    <a href="{{ path('reservation_edit', {'idReservation': reservation.idReservation}) }}">edit</a>

    {{ include('reservation/_delete_form.html.twig') }}
{% endblock %}
{% endblock %}
