{% extends './backend/backbody.html.twig' %}

        {% block title %}{% endblock %}

        {% block content %}
            {% if Reservationresto  %}
                <div class="card">
                <div class="card-header">
                    <h3 class="card-title">LISTE DES RESERVATIONS</h3>
                </div>
                <div class="card-body p-0">
                    <table id="chauffeur" class="table table-striped">
                        <thead>
                        <tr>

                            <th>Resto</th>
                            <th>nbre place reserver</th>

                            <th>mail</th>
                            <th>numero</th>

                        </tr>
                        </thead>
                        <tbody>
                        {% for r in Reservationresto %}
                            <tr>

                                <td>{{ r.idResto.nomresto }}</td>
                                <td>{{ r.Nbpersonne }}</td>
                                <td>{{ r.mail }}</td>
                                <td>{{ r.numero }}</td>

                                {#         <td>
                            <a class="btn btn-block btn-info" href="{{ path('detail_hotel',{'idHotel':h.idHotel}) }}" > detail</a>

                        </td>
                        <td>
                            <a class="btn btn-block btn-success" href="{{ path('edit_hotel',{'idHotel':h.idHotel}) }}" > modifier</a>
                        </td>
                        <td>
                            <a class="btn btn-block btn-danger" href="{{ path('delete_hotel',{'idHotel':h.idHotel}) }} " >supprimer </a>
                        </td>

                        </td>
                                #}
                            </tr>

                        {% endfor %}
                        </tbody>

                    </table>
                </div>

            {% else %}
                <div class="container">
                    <table>
                        <thead>
                        <th>aucune reservation</th>

                        </thead>
                    </table>

                </div>

            {% endif %}
        {% endblock content %}
