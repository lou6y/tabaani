{% extends './backend/backbody.html.twig' %}
{% block content %}

    {{ form_start(form,{attr:{'id':'form'}}) }}
<div class="container">
    <div class="form-group">
        {{ form_label(form.idHotel,"hotelid:" ) }}

        {{ form_widget(form.idHotel,{attr: {class:'form-control'}}) }}


    </div>
    <div class="form-group">
        {{ form_label(form.categ,"category :") }}

        {{ form_widget(form.categ, {attr: {class:'form-control'}}) }}
    </div>
    <div class="form-group">
        {{ form_label(form.type,"type:" ) }}

        {{ form_widget(form.type,{attr: {class:'form-control'}}) }}
    </div>

<div class="card-footer">
    <button type="submit" href="{{ path('add_service') }}" class="btn btn-primary">Submit</button>
</div>





{{ form_end(form) }}
{% endblock content %}