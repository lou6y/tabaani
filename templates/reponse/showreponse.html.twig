{% extends './backend/backbody.html.twig' %}
{% block content %}
    <div class="card">
    <div class="card-header">
        <h3 class="card-title">Liste des reponses</h3>
    </div>

    <div class="card-body p-0">
        <table id="chauffeur" class="table table-striped">
            <thead>
            <tr>
                <th>date reponse </th>
                <th>description</th>
            </tr>
            </thead>
            <tr>
                <td>{{ rep.DateRep |date("m/d/Y") }}</td>
                <td>{{ rep.Description }}</td>
            </tr>
        </table>
        <div align="center">
            <a class="btn btn-block btn-info" href="{{ path('list_reclamationback') }}" >retour</a>
        </div>
    </div>

{% endblock content %}